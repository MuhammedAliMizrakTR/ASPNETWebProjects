@model IEnumerable<CoreMVCLibraryProject.Models.Entities.Yazar>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.YazarAd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.YazarSoyad)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.YazarAd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.YazarSoyad)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.YazarNo">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.YazarNo">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.YazarNo">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
